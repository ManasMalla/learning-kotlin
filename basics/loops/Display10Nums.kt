/**
 * A simple kotlin program to display the half of numbers from 0 to 10.
 * Note: When using if-else statements or loops, the curly braces are optional 
 * if the block has only one statement.
 */

fun main() {
    for(i in 0..10)
    println(i/2)
}